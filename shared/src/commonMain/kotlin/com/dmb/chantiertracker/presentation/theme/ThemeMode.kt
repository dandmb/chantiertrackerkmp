package com.dmb.chantiertracker.presentation.theme

// The user's theme choice — persisted in AppPreferences. `System` follows the
// OS light/dark setting (`isSystemInDarkTheme`), the two others force it.
enum class ThemeMode { System, Light, Dark }
